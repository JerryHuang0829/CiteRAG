global:
  scrape_interval: 5s
scrape_configs:
  - job_name: citerag
    metrics_path: /metrics
    static_configs:
      # 抓在 host 上跑的 CiteRAG（uvicorn api:app --port 8000）
      - targets: ["host.docker.internal:8000"]
