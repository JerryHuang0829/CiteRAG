# P3-A 選配依賴（LangGraph 版 agent + tracing）——不進核心 requirements/Docker，保持部署精簡。
# 安裝：pip install -r requirements-langgraph.txt
langgraph==1.2.7
langchain-core==1.4.8
langfuse==4.12.0          # tracing（env-gated：有 LANGFUSE_PUBLIC_KEY 才開）
