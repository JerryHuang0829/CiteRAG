# CiteRAG — 核心執行期依賴（pin 至本機實測版本）
# 注意：LLM/VLM 推論走外部 Ollama 服務（qwen3:4b-instruct / gemma3:4b），非 pip 安裝。
# 統計(stats.py)、BM25、引用護欄為純 stdlib，無額外依賴。

# 核心 RAG 引擎
fastembed==0.8.0              # embedding(bge-small-zh) + reranker(bge-reranker-base) ONNX, CPU
faiss-cpu==1.14.3             # 向量索引(IndexFlatIP)
pymupdf4llm==1.27.2.3         # PDF → markdown(帶頁碼)
langchain-text-splitters==1.1.2
numpy==2.5.0

# Web UI / API
gradio==6.19.0               # app.py 三分頁 UI
fastapi==0.138.0             # api.py REST API + 自製前端
uvicorn==0.49.0
pydantic==2.13.4

# 可選：pgvector 向量資料庫後端（CITERAG_PGVECTOR=1 時才需要；預設用 FAISS）
# psycopg2-binary==2.9.12

# 開發/測試：pip install pytest（見 .github/workflows/ci.yml）
