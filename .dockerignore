# 快取（image 內下載）。index 改為 commit 進 repo（HF 免費 build 記憶體無法 embed 9.6k chunks），只留這兩檔進 build context
index/*
!index/chunks.jsonl
!index/faiss.index
hf_cache/
models/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 祕密（用 HF Space secrets 注入，勿 bake）
.env
.env.*

# 執行期不需要（縮小 image）
.git/
.github/
tests/
w0/
*.md
start_app.bat
.findata_cache.json
